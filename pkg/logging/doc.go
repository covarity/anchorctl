/*
Package logging is a wrapper around the logger tool. Logrus is the logging solution
currently implemented for the covarity suite. The logging package provides the flexibility
of changing log solution without having to change the code in other packages.

Log package also provides commonly used functions such as logging with single field, and logging
with multiple fields.
*/
package logging
